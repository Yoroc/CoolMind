apiVersion: v1
kind: Service
metadata:
  name: {{ include "coolmind.fullname" . }}
  labels:
    {{- include "coolmind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coolmind.selectorLabels" . | nindent 4 }}

